# Global configurations shared across all sub-charts
global:
  environment: production
  namespace: titan-x
  imageRegistry: "docker.io/titanx"

# Gateway / Load Balancer
gateway:
  replicaCount: 2
  image:
    repository: gateway
    tag: latest
  service:
    type: LoadBalancer
    port: 80

# Distributed Web Crawlers Nodes
crawlerNode:
  replicaCount: 10
  image:
    repository: crawler-node
    tag: latest
  resources:
    limits:
      cpu: "2"
      memory: 4Gi
    requests:
      cpu: "500m"
      memory: 1Gi
  env:
    REDIS_URL: "redis://redis-master.titan-x.svc:6379/0"
    KAFKA_BOOTSTRAP_SERVERS: "kafka.titan-x.svc:9092"
    CLICKHOUSE_HOST: "clickhouse.titan-x.svc"

# AI Extraction Workers (GPU resources requested)
aiWorker:
  replicaCount: 3
  image:
    repository: ai-worker
    tag: latest
  resources:
    limits:
      nvidia.com/gpu: 1
      memory: 16Gi
    requests:
      nvidia.com/gpu: 1
      memory: 8Gi
  env:
    RAY_CLUSTER_ADDRESS: "ray://ray-head.titan-x.svc:6379"
    QDRANT_HOST: "qdrant.titan-x.svc"
    NEO4J_URI: "bolt://neo4j.titan-x.svc:7687"

# External services configuration (if not deploying within cluster)
databases:
  postgres:
    enabled: true
    host: "titan-postgres-instance"
    database: "titanx"
  redis:
    enabled: true
  kafka:
    enabled: true
